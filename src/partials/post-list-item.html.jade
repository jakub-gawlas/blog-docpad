// Partial list item of posts
// parameters:
//      post: list item

.post-preview
    a(href= post.url)
        h2.post-title= post.title
        if post.subtitle
            h3.post-subtitle= post.subtitle
    p.post-meta
        | Posted by #{post.author} on #{post.date.toLocaleDateString()}
hr
